<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
     <a class="navbar-brand" href="{{ path('app_home') }}"> 
        RobyMarket
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if not app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">
                        <i class="fa fa-sign-in-alt"></i> 
                    </a>
                </li>
                <li class="nav-item">
                   
                        <i class="fa fa-user-plus"></i>
                    </a>
                </li>
            {% else %}
                <li class="nav-item">
                    {# <a class="nav-link" href="{{ path('app_produit_index') }}"> #}
                        {{ app.user.username }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        <i class="fa fa-sign-out-alt"></i>
                    </a>
                </li>

            {% endif %}
            
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Produits
                                
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                         <a class="dropdown-item" href="{{ path('admin') }}"> 
                            Liste
                        </a>
                        {# <a class="dropdown-item" href="{{ path('app_admin_produit_new') }}"> #}
                            Ajouter
                        </a>
                    </div>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Clients
                                
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {# <a class="dropdown-item" href="{{ path('app_user_index') }}"> #}
                            Liste
                        </a>
                        <a class="dropdown-item" href="{{ path('app_user_new') }}">
                            Ajouter
                        </a>
                    </div>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Commandes
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {# <a class="dropdown-item" href="{{ path('app_admin_commande_index') }}"> #}
                            Liste
                        </a>
                        {# <a class="dropdown-item" href="{{ path('app_admin_commande_new') }}"> #}
                            Ajouter
                        </a>
                    </div>
                </li>
            {% endif %}

              {% if app.user %}
                                    <li class="dropdown">
                                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" id="user">
                                            <i class="fa fa-user" aria-hidden="true"></i>
                                            <span class="caret"></span>
                                            {# <span class="sr-only">{{ app.username }}</span> #}
                                        </a>
                                        <ul class="dropdown-menu user" role="menu" aria-labelledby="user">
                                            <li>
                                                 <a href="{{ path('app_user_edit') }}"> 
                                                    <i class="fa fa-edit" aria-hidden="true"></i> {{ 'menu.user'|trans }}
                                                </a>
                                            </li>
                                            <li class="divider"></li>
                                            <li>
                                                {# <a href="{{ path('security_logout') }}"> #}
                                                    <i class="fa fa-sign-out" aria-hidden="true"></i> {{ 'menu.logout'|trans }}
                                                </a>
                                            </li>
                                        </ul>
                                    </li>
                                {% endif %}

            <li class="nav-item active">
                 <a class="nav-link" href="{{ path('app_panier') }}"> 
                    <i class="fa fa-shopping-cart"></i> 
                </a>
            </li>
        </ul>

        {# <form class="form-inline my-2 my-lg-0" action="{{ path('app_recherche') }}"> #}
            <input name="search" class="form-control mr-sm-2 col-3" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
                    Search
                </button>
            </form>
        </div>
    </nav>
    